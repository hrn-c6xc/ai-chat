{
  "groups": [
    {
      "title": "Getting Started",
      "items": [
        {
          "id": "intro",
          "label": "Introduction",
          "icon": "home",
          "badge": "v2.1",
          "content": "# Introduction\n\nWelcome to the official documentation for the **Titan Framework**. This guide is designed to help you set up, configure, and deploy your first application in minutes.\n\nTitan is built on modern architecture principles, ensuring high availability and fault tolerance. Whether you are building a small microservice or a large-scale enterprise application, Titan provides the tools you need.\n\n## Core Philosophy\n\n1. **Simplicity First**: Minimal configuration required to start.\n2. **Scalability**: Built-in support for horizontal scaling.\n3. **Developer Experience**: Comprehensive CLI tools and hot-reloading.\n\n> **Note**: This documentation assumes you have a basic understanding of TypeScript and Node.js."
        },
        {
          "id": "installation",
          "label": "Installation",
          "icon": "download",
          "badge": "Setup",
          "content": "# Installation\n\nGetting started with Titan is straightforward. Ensure you have **Node.js v18** or higher installed on your system.\n\n### Step 1: Clone the Repository\n\n```bash\ngit clone https://github.com/titan-framework/starter.git my-project\ncd my-project\n```\n\n### Step 2: Install Dependencies\n\nWe recommend using `pnpm` for faster installation times, but `npm` works just as well.\n\n```bash\npnpm install\n# or\nnpm install\n```\n\n### Step 3: Environment Variables\n\nCopy the example environment file to configure your local settings.\n\n```bash\ncp .env.example .env\n```\n\nOpen the `.env` file and update the `DATABASE_URL` and `SECRET_KEY`."
        },
        {
          "id": "project-structure",
          "label": "Project Structure",
          "icon": "folder-tree",
          "badge": "Architecture",
          "content": "# Project Structure\n\nUnderstanding the file structure is key to mastering the framework.\n\n```\nmy-project/\n├── app/\n│   ├── controllers/    # Route handlers\n│   ├── models/         # Database schemas\n│   └── services/       # Business logic\n├── config/\n│   └── app.config.ts   # Main configuration\n├── public/             # Static assets\n├── tests/              # Test suites\n└── package.json\n```\n\n### The `app` Directory\n\nThis is where the magic happens. All your application logic resides here. Keep your controllers lean by moving complex logic to **services**."
        }
      ]
    },
    {
      "title": "Core Concepts",
      "items": [
        {
          "id": "routing",
          "label": "Routing System",
          "icon": "route",
          "badge": "Core",
          "content": "# Routing System\n\nTitan uses a centralized routing system that supports dynamic parameters and middleware injection.\n\n### Basic Routes\n\nDefine routes in your controller files using decorators or the fluent API.\n\n```typescript\nimport { Controller, Get } from '@titan/core';\n\n@Controller('/users')\nexport class UserController {\n  \n  @Get('/')\n  index() {\n    return { message: 'List of users' };\n  }\n\n  @Get('/:id')\n  show(req: Request) {\n    const { id } = req.params;\n    return { userId: id };\n  }\n}\n```\n\n### Route Groups\n\nYou can group routes to apply middleware or prefixes collectively.\n\n```typescript\nRouter.group('/api', () => {\n  // Routes defined here will have /api prefix\n}).middleware([AuthMiddleware]);\n```"
        },
        {
          "id": "middleware",
          "label": "Middleware",
          "icon": "shield-check",
          "badge": "Security",
          "children": [
            {
              "id": "auth-middleware",
              "label": "Authentication",
              "icon": "lock",
              "badge": "JWT",
              "content": "# Authentication Middleware\n\nSecure your API endpoints using JWT (JSON Web Tokens). Titan provides a built-in guard for token validation.\n\n### Implementation\n\nAttach the guard to your routes to enforce authentication.\n\n```typescript\nimport { AuthGuard } from '@titan/auth';\n\n@UseGuards(AuthGuard)\n@Controller('/profile')\nexport class ProfileController {\n  // Only accessible with valid token\n}\n```\n\n### Custom Strategy\n\nYou can implement custom strategies by extending the base `Strategy` class.\n\n```typescript\nexport class ApiKeyStrategy extends Strategy {\n  validate(req: Request) {\n    const key = req.headers['x-api-key'];\n    if (!key) throw new UnauthorizedError();\n    return { id: key };\n  }\n}\n```"
            },
            {
              "id": "logging-middleware",
              "label": "Logging",
              "icon": "terminal",
              "badge": "Ops",
              "content": "# Logging Middleware\n\nGain visibility into your application's request lifecycle. The logging middleware automatically captures request duration, status codes, and error stacks.\n\n### Configuration\n\nEnable logging globally in your `app.config.ts`.\n\n```typescript\nexport default {\n  logging: {\n    level: 'debug',\n    format: 'json', // or 'text'\n    output: ['console', 'file']\n  }\n}\n```\n\n### Output Example\n\n```json\n{\n  \"level\": \"info\",\n  \"method\": \"GET\",\n  \"url\": \"/api/users\",\n  \"status\": 200,\n  \"duration\": \"12ms\"\n}\n```"
            }
          ]
        }
      ]
    },
    {
      "title": "Database",
      "items": [
        {
          "id": "orm",
          "label": "Active Record ORM",
          "icon": "database",
          "badge": "Data",
          "content": "# Active Record ORM\n\nTitan includes a powerful ORM inspired by Laravel Eloquent and Active Record patterns. It supports PostgreSQL, MySQL, and SQLite.\n\n### Defining Models\n\nModels represent tables in your database.\n\n```typescript\nimport { Model, Column, PrimaryKey } from '@titan/orm';\n\nexport class User extends Model {\n  @PrimaryKey()\n  id: number;\n\n  @Column()\n  email: string;\n\n  @Column({ name: 'created_at' })\n  createdAt: Date;\n}\n```\n\n### Querying Data\n\nFetching data is intuitive and chainable.\n\n```typescript\n// Find all active users\nconst users = await User.where('active', true).get();\n\n// Find by ID\nconst user = await User.find(1);\n```"
        },
        {
          "id": "migrations",
          "label": "Migrations",
          "icon": "git-merge",
          "badge": "Schema",
          "content": "# Database Migrations\n\nVersion control your database schema. Migrations allow you to modify your database structure safely.\n\n### Creating a Migration\n\nUse the CLI to generate a new migration file.\n\n```bash\ntitan make:migration create_posts_table\n```\n\n### Migration Structure\n\nThe file contains an `up` method to apply changes and a `down` method to revert them.\n\n```typescript\nexport class CreatePostsTable {\n  async up(schema: Schema) {\n    await schema.createTable('posts', (table) => {\n      table.increments('id');\n      table.string('title');\n      table.text('content');\n      table.timestamps();\n    });\n  }\n\n  async down(schema: Schema) {\n    await schema.dropTable('posts');\n  }\n}\n```"
        }
      ]
    },
    {
      "title": "Advanced",
      "items": [
        {
          "id": "caching",
          "label": "Caching Strategies",
          "icon": "zap",
          "badge": "Performance",
          "content": "# Caching Strategies\n\nImprove response times by caching frequently accessed data. Titan supports Redis and in-memory caching.\n\n### Basic Usage\n\n```typescript\nimport { Cache } from '@titan/cache';\n\n// Store data for 60 minutes\nawait Cache.put('user:1', userData, 60);\n\n// Retrieve data\nconst user = await Cache.get('user:1');\n\n// Remove data\nawait Cache.forget('user:1');\n```\n\n### Cache Aside Pattern\n\nThis pattern involves checking the cache first; if data is missing, fetch from DB and populate the cache.\n\n```typescript\nlet user = await Cache.get('user:1');\nif (!user) {\n  user = await User.find(1);\n  await Cache.put('user:1', user);\n}\nreturn user;\n```"
        },
        {
          "id": "queues",
          "label": "Job Queues",
          "icon": "list-checks",
          "badge": "Async",
          "content": "# Job Queues\n\nOffload heavy processing tasks to a background queue to keep your API snappy. Perfect for sending emails or processing images.\n\n### Defining Jobs\n\n```typescript\nimport { Job, Dispatch } from '@titan/queue';\n\n@Job('SendEmail')\nexport class SendEmailJob {\n  async handle(data: any) {\n    // Logic to send email\n    console.log(`Sending email to ${data.to}`);\n  }\n}\n```\n\n### Dispatching Jobs\n\n```typescript\nimport { Dispatch } from '@titan/queue';\n\nawait Dispatch(new SendEmailJob({ to: 'user@example.com' }));\n```"
        }
      ]
    },
    {
      "title": "Deployment",
      "items": [
        {
          "id": "docker",
          "label": "Docker",
          "icon": "container",
          "badge": "DevOps",
          "content": "# Docker Deployment\n\nWe provide a pre-configured `Dockerfile` optimized for production builds.\n\n### Build the Image\n\n```bash\ndocker build -t my-titan-app .\n```\n\n### Run the Container\n\n```bash\ndocker run -p 3000:3000 -e DATABASE_URL=postgres://user:pass@db:5432/db my-titan-app\n```\n\n### Dockerfile\n\n```dockerfile\nFROM node:18-alpine AS builder\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci --only=production\nCOPY . .\nRUN npm run build\n\nFROM node:18-alpine\nWORKDIR /app\nCOPY --from=builder /app /app\nEXPOSE 3000\nCMD [\"node\", \"dist/main.js\"]\n```"
        },
        {
          "id": "cloud",
          "label": "Cloud Providers",
          "icon": "cloud",
          "badge": "Cloud",
          "content": "# Cloud Deployment\n\nTitan is platform-agnostic and can be deployed to AWS, Google Cloud, Azure, or Vercel.\n\n### Vercel (Serverless)\n\nSimply connect your repository to Vercel. It will auto-detect the configuration.\n\n### AWS EC2\n\nUse PM2 to manage the process on your EC2 instance.\n\n```bash\nnpm install -g pm2\npm2 start dist/main.js --name titan-app\n```"
        }
      ]
    }
  ]
}
